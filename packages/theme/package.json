{
  "name": "@miraui/theme",
  "version": "1.0.0",
  "main": "src/index.ts",
  "files": [
    "src/**/*"
  ],
  "scripts": {
    "build": "tsup src --dts",
    "build:fast": "tsup src",
    "dev": "pnpm build:fast --watch",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "tailwindcss": "^4.0.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0"
  }
}